name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '22 11 * * 4'  # Cada viernes a las 11:22 AM UTC

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: 'javascript-typescript'
            build-mode: 'none'
          - language: 'python'
            build-mode: 'none'
          # Agrega más lenguajes según sea necesario
          # Por ejemplo, para C/C++:
          # - language: 'c-cpp'
          #   build-mode: 'autobuild'
          # Para C#:
          # - language: 'csharp'
          #   build-mode: 'autobuild'
          # Para Java/Kotlin:
          # - language: 'java-kotlin'
          #   build-mode: 'autobuild'
          # Para Go:
          # - language: 'go'
          #   build-mode: 'autobuild'
          # Para Swift:
          # - language: 'swift'
          #   build-mode: 'autobuild'

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}
        # Habilita el caché de dependencias para mejorar el rendimiento
        dependency-caching: 'true'

    - name: Autobuild
      if: ${{ matrix.build-mode == 'autobuild' }}
      uses: github/codeql-action/autobuild@v4

    - name: Build C/C++ code
      if: ${{ matrix.build-mode == 'manual' && matrix.language == 'c-cpp' }}
      run: |
        # Reemplaza estos comandos con los comandos de compilación adecuados para tu proyecto
        echo 'make bootstrap'
        echo 'make release'
        exit 1

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:${{matrix.language}}"

    - name: Upload SARIF results
      uses: github/codeql-action/upload-sarif@v4
      with:
        sarif_file: ${{ github.workspace }}/**/*.sarif
